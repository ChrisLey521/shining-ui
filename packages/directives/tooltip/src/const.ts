import { Placement } from 'constants';
import { TooltipProps } from './type';

const DEFAULT_PLACEMENT = Placement.TopStart

const DEFAULT_TOOLTIP_PROPS: TooltipProps = {
    offset: 10,
    placement: DEFAULT_PLACEMENT
}

export { DEFAULT_TOOLTIP_PROPS };

