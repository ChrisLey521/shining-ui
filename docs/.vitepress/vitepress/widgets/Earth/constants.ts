import type { EarthConfig } from "./types";


const defaultConfig: EarthConfig = {
    width: 800,
    height: 600,
    earthRadius: 125,
    networkLayerRadius: 130,
    energyFieldRadius: 135,
    pulseLayerRadius: 140,
    pulsePointRadius: 2,
    starCount: 1000,
    starSize: 1,
    earthColor: '#1a1a2e',
    networkColor: '#16213e',
    energyColor: '#0f3460',
    pulseColor: '#e94560',
    starColor: '#ffffff',
    borderColor: '#e94560',
    glowColor: '#e94560'
  };

  export {
    defaultConfig
  }